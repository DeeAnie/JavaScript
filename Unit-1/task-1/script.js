// 1. Простое сообщение: сохраните текстовое сообщение в переменной и выведите его в браузере
let messege = "Hello Kelechek";
alert(messege);           //The alert() method displays an alert box with a message and an OK button.
document.write(messege);  //The document.write() method writes directly to an open (HTML) document stream.
console.log(messege);     //The console.log() method writes (logs) a message to the console.
